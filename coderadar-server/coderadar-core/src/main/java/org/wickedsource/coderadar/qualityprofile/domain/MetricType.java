package org.wickedsource.coderadar.qualityprofile.domain;

public enum MetricType {
	/** The higher the metric value, the better. */
	COMPLIANCE,

	/** The higher the metric value, the worse. */
	VIOLATION
}
